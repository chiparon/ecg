# 第二阶段实验方案：四种训练策略对未见采集扰动的泛化

## 1. 实验目标

第二阶段不再主要回答“结构化噪声是否影响评价”，而是回答：

> **使用采集机制一致的电极空间扰动进行训练，是否能够提高多导联 ECG 模型对未见噪声强度、未见电极组合和未见噪声实现的泛化能力？**

核心比较对象为四种训练策略：

1. `clean-only`；
2. `independent-noise augmentation`；
3. `electrode-noise augmentation`；
4. `mixed augmentation`。

第二阶段必须避免训练集和测试集使用完全相同的噪声分布。否则只能证明模型记住了训练扰动，不能证明采集机制一致增强具有泛化价值。

---

## 2. 总体实验原则

四种策略必须满足以下控制条件：

- 使用相同的 PTB-XL 分析队列和官方 patient-level fold；
- 使用相同的模型架构、参数量、优化器、学习率和训练 epoch 上限；
- 使用相同的 batch size、数据加载顺序规则和随机种子集合；
- 使用相同的 clean validation 集选择最佳 checkpoint；
- 不在 noisy validation 或 noisy test 上调阈值；
- 四种策略使用相同的测试矩阵；
- 每个训练样本的总曝光次数尽量相同；
- 保存逐记录预测，不能只保存汇总指标；
- 主结果至少使用 ResNet 和 TCN 两种模型；
- 主结果至少使用 3 个训练随机种子，推荐 5 个；
- 任何看到测试结果后改变的增强比例、SNR 或电极组合，都必须标记为探索性实验，不得再称为主结果。

推荐的主实验规模为：

```text
4种训练策略 × 2种模型 × 5个训练种子 = 40次训练
```

如果计算资源不足，先执行：

```text
4种训练策略 × 2种模型 × 3个训练种子 = 24次训练
```

建议先做小规模 pilot，确认四种策略均能正常训练和保存 checkpoint，再运行 full phase 2。

---

## 3. 数据和任务固定设置

### 3.1 数据集

继续使用 PTB-XL 1.0.3 的 100 Hz 版本，采用第一阶段已经确定的 21388 条分析队列：

| 分区 | 记录数 | 说明 |
|---|---:|---|
| Train | 17084 | 官方 `strat_fold` 1–8 |
| Validation | 2146 | 官方 `strat_fold` 9 |
| Test | 2158 | 官方 `strat_fold` 10 |

411 条缺少目标 diagnostic superclass 标签的记录继续排除，并在所有四种策略中保持完全一致。不得在第二阶段重新构造数据划分。

### 3.2 标签

继续使用五维多标签 diagnostic superclass：

```text
[NORM, MI, STTC, CD, HYP]
```

使用 `BCEWithLogitsLoss`。标签仍然允许共现，不转为互斥五分类。

### 3.3 预处理

必须完全复用第一阶段的预处理：

- 100 Hz；
- 每条记录 1000 个时间点；
- 12 导联固定顺序；
- mV 空间去每导联时间均值；
- 不额外滤波；
- 不逐导联标准化；
- 使用第一阶段训练集拟合的全局 RMS 标量 `0.2254905872 mV`；
- 先在 mV 空间添加噪声，再除以全局 RMS 标量。

任何改变预处理的实验都应作为单独敏感性分析，不得混入四策略主比较。

---

## 4. 四种训练策略的精确定义

### 4.1 策略一：Clean-only

训练输入始终为：

\[
X_{train}=X.
\]

这是真实的普通训练基线。

每个 epoch 中，每条 ECG 被使用一次，不加入任何人工噪声。为了保证四种策略的样本曝光公平，其他三种策略也必须在每个 epoch 中让每条原始 ECG 只被采样一次，只是其中一部分样本被替换为受扰动版本。

配置名称：

```yaml
strategy: clean_only
augmentation_probability: 0.0
```

---

### 4.2 策略二：Independent-noise augmentation

训练输入为：

\[
X_{train}=X+n_{ind}
\]

但不能让所有训练样本都被加噪。推荐使用混合批次：

- 50% clean；
- 50% independent-noise augmented。

设每个训练样本的增强指示变量为：

\[
z\sim Bernoulli(0.5).
\]

则：

\[
X_{train}=X+z n_{ind}.
\]

这样可以避免模型完全牺牲 clean 输入性能，同时保证与 electrode augmentation 和 mixed augmentation 的训练曝光量可比。

主实验使用 `independent_rms`，而不是未匹配 RMS 的原始 independent。原因是第一阶段已经显示，主问题应尽量排除逐导联能量差异。

推荐训练噪声分布：

- SNR：20 dB 或 10 dB，各占 50%；
- 噪声基础实现：新的训练 noise seed，与测试 noise seed 完全隔离；
- 主训练噪声：带限 Gaussian，0.5–40 Hz；
- 每次采样动态生成，或使用确定性 seed 生成；
- 逐导联 RMS 按 electrode 目标逐导联 RMS 做匹配；
- 不使用训练测试共享的噪声数组。

配置名称：

```yaml
strategy: independent_rms
augmentation_probability: 0.5
train_snr_db: [20, 10]
train_snr_probability: [0.5, 0.5]
noise_space: lead
rms_match_target: electrode
```

---

### 4.3 策略三：Electrode-noise augmentation

训练输入为：

\[
X_{train}=X+A\epsilon_e.
\]

同样使用 50% clean 与 50% electrode-noise augmented 的混合训练。

电极空间顺序继续使用：

```text
[RA, LA, LL, V1, V2, V3, V4, V5, V6]
```

导联空间顺序继续使用：

```text
[I, II, III, aVR, aVL, aVF, V1, V2, V3, V4, V5, V6]
```

使用第一阶段经过验证的 WCT 矩阵：

\[
A=
\begin{bmatrix}
B & 0_{6\times6}\\
-\frac13\mathbf 1_{6\times3} & I_6
\end{bmatrix}.
\]

训练电极组合必须进行预注册。推荐主设置如下：

| 电极组合 | 训练采样概率 |
|---|---:|
| RA、LA、LL 同时扰动 | 0.25 |
| V1–V3 中随机选择 1 个 | 0.25 |
| V4–V6 中随机选择 1 个 | 0.25 |
| 随机选择 2–4 个电极 | 0.25 |

每次选中的电极扰动使用相同的电极空间基础噪声模板，再通过 `A` 映射。对于未被选中的电极，扰动为零。

训练 SNR 继续使用：

- 20 dB，占 50%；
- 10 dB，占 50%。

主训练不使用 0 dB，因为 0 dB 在第一阶段属于极强压力条件，适合作为测试压力点；如果训练也大量使用 0 dB，可能使模型针对极端条件过拟合，并削弱对未见强度的泛化解释。

配置名称：

```yaml
strategy: electrode
augmentation_probability: 0.5
train_snr_db: [20, 10]
train_snr_probability: [0.5, 0.5]
electrode_combo_distribution:
  limb_all: 0.25
  precordial_v1_v3_single: 0.25
  precordial_v4_v6_single: 0.25
  random_2_to_4: 0.25
```

---

### 4.4 策略四：Mixed augmentation

Mixed augmentation 不是简单把 independent 与 electrode 两种噪声相加，也不能使用测试结果后随意选择比例。主实验预先固定为：

- 50% clean；
- 25% independent RMS-matched noise；
- 25% electrode-mapped noise。

即：

\[
X_{train}=X+z_1 n_{ind}+z_2 A\epsilon_e,
\]

其中每条样本只激活一种增强，不能同时激活两种噪声：

\[
P(z_1=1)=0.25,
\quad
P(z_2=1)=0.25,
\quad
P(z_1=z_2=1)=0.
\]

这样 mixed 与两个单一增强策略具有相同的总加噪比例，也不会因为噪声叠加而引入额外 SNR 混杂。

推荐配置：

```yaml
strategy: mixed
clean_probability: 0.5
independent_probability: 0.25
electrode_probability: 0.25
train_snr_db: [20, 10]
train_snr_probability: [0.5, 0.5]
```

建议增加一个仅作为敏感性分析的版本：

- 25% clean；
- 37.5% independent；
- 37.5% electrode。

但该版本不能替代主 mixed 配置，也不能在主结果完成后根据性能选择。

---

## 5. 训练分布和测试分布设计

## 5.1 主训练分布

主训练分布预注册为：

| 变量 | 训练设置 |
|---|---|
| Clean 比例 | 50% |
| Independent 策略加噪比例 | 50% |
| Electrode 策略加噪比例 | 50% |
| Mixed 中 clean 比例 | 50% |
| Mixed 中 independent 比例 | 25% |
| Mixed 中 electrode 比例 | 25% |
| SNR | 20 dB、10 dB |
| SNR 比例 | 各 50% |
| 主噪声 | 0.5–40 Hz 带限 Gaussian |
| 训练 noise seed | 10000–19999 范围派生 |
| 测试 noise seed | 20000–29999 范围派生，严格隔离 |

训练时每个 epoch 对每条 ECG 生成一次或确定性抽取一次扰动。若使用动态生成，必须通过 `epoch、record_id、strategy、model-independent noise seed` 派生随机数，避免不同策略由于随机数消耗顺序不同而产生不可控差异。

### 5.2 测试矩阵

测试必须使用相同测试输入矩阵，不因训练策略改变。建议包含以下条件：

#### A. Clean test

确认增强训练是否损害无噪声性能。

#### B. Seen-SNR test

- 20 dB；
- 10 dB。

用于衡量训练分布内性能，但不是泛化主结果。

#### C. Unseen-SNR test

- 15 dB；
- 5 dB；
- 0 dB作为压力测试。

其中 15 dB 和 5 dB 是主泛化测试点，0 dB 是极强压力测试点。

#### D. Unseen electrode-combination test

训练组合与测试组合必须完全分离。推荐测试组合：

| 测试组合 | 训练中是否出现 |
|---|---:|
| RA 单独 | 否 |
| LA 单独 | 否 |
| LL 单独 | 否 |
| V1 单独 | 否 |
| V6 单独 | 否 |
| RA+V1 | 否 |
| LA+V6 | 否 |
| LL+V4 | 否 |
| RA+LA+V1+V6 | 否 |
| 随机未见 3 电极组合 | 否 |

为了避免“随机 2–4 个”训练分布无意中覆盖所有组合，主实验应把训练组合明确限定为预注册集合，而不是任意随机组合。建议使用以下主训练集合：

```text
train_combos = {
  [RA, LA, LL],
  [V1], [V2], [V3],
  [V4], [V5], [V6],
  [RA, LA], [LA, LL], [RA, LL]
}
```

测试集合使用与上述集合不相同的单电极和组合。

#### E. Unseen noise realization test

使用测试 noise base：

```text
20001, 20002, 20003, 20004, 20005
```

训练中不能使用这些 seed。所有策略使用同一批测试 noise base。

#### F. Optional NSTDB sensitivity test

使用 `bw`、`ma`、`em` 进行探索性回放。由于第一阶段已经确认 NSTDB 存在 PSD 和时序混杂，NSTDB 不能作为唯一主要结论来源。主训练策略仍使用结构可控的 Gaussian；NSTDB 只用来观察外部噪声回放下的方向是否保持。

---

## 6. 推荐超参数

### 6.1 ResNet 和 TCN 结构

完全复用第一阶段，不得因为某个策略表现较差而单独调整模型：

| 模型 | 设置 |
|---|---|
| ResNet | 1D ResNet18 风格，block 2/2/2/2，base channel 24 |
| TCN | channel 24/24/48/48/96/96，dilation 1/2/4/8/16/32，kernel 3 |
| 输出 | 5 个 sigmoid logits |
| 损失 | BCEWithLogitsLoss |
| ResNet 参数量 | 545717 |
| TCN 参数量 | 134885 |

### 6.2 优化器和训练预算

主配置：

```yaml
optimizer: AdamW
learning_rate: 0.001
weight_decay: 0.0001
batch_size: 128
max_epochs: 25
scheduler: none
mixed_precision: false
allow_tf32: false
cudnn_deterministic: true
gradient_clip_norm: 5.0
```

是否加入梯度裁剪必须四种策略一致。若第一阶段没有使用 gradient clipping，第二阶段主实验也建议不新增；如果训练时出现 NaN，可以统一设置四种策略为 `clip_norm=5.0`，并在配置中记录。

建议使用：

```yaml
selection_metric: clean_validation_macro_auroc
early_stopping: false
checkpoint: best_clean_validation
```

主实验最好保持 25 个 epoch 完整训练，并保存每个 epoch 的 checkpoint。最终模型使用 clean validation Macro-AUROC 最佳 epoch，而不是 noisy validation 最佳 epoch。

### 6.3 随机种子

至少使用：

```text
17, 29, 43
```

推荐增加：

```text
101, 202
```

随机性必须拆分为：

```text
training_seed
augmentation_seed
noise_seed
bootstrap_seed
```

训练策略之间可以使用相同的 `training_seed`，但不得共享同一个随机数流状态。应通过显式 `SeedSequence` 派生子种子，避免某个策略因调用随机函数次数不同而影响后续数据顺序。

### 6.4 增强概率

主配置固定为：

```yaml
clean_probability: 0.5
augmentation_probability: 0.5
```

不要使用 100% 加噪作为主实验。100% 加噪可能导致模型在 clean 输入上的性能下降，也会使四种策略的比较混入输入分布暴露程度差异。

---

## 7. 训练和验证流程

### 7.1 每个 epoch 的样本调度

建议每个 epoch：

1. 对训练集按照固定 seed 洗牌一次；
2. 每条 ECG 只出现一次；
3. 根据策略和 augmentation probability 决定输入类型；
4. 噪声在进入模型前生成；
5. 保存本 epoch 的 clean/independent/electrode 样本比例和实际 SNR 统计；
6. 不改变标签；
7. 不使用 noisy validation 选择模型。

### 7.2 Validation

默认使用 clean validation：

\[
V_{clean}=\{X_{val},y_{val}\}.
\]

每个 epoch 计算：

- Macro-AUROC；
- Macro-AP；
- Macro-F1；
- BCE loss。

以 clean validation Macro-AUROC 选择 best checkpoint。为观察增强训练是否损害 clean 性能，可以额外记录 noisy validation，但不能用于主 checkpoint 选择。

### 7.3 阈值

每个模型和训练策略的五个标签阈值只在 clean validation 上选择一次。测试时所有条件使用相同阈值。

不要分别为每个 noisy test 条件重新调阈值，否则 F1 不能反映真实部署时的固定阈值退化。

---

## 8. 主要测试矩阵

所有训练策略使用同一批测试样本、同一批测试噪声实例、同一组阈值和同一组电极组合。

推荐主测试矩阵：

| 测试组 | SNR | 噪声类型 | 电极组合 | 解释 |
|---|---|---|---|---|
| clean | — | 无 | — | clean 性能代价 |
| seen independent | 20/10 | independent_rms | all-lead | 分布内基线 |
| seen electrode | 20/10 | electrode | train combos | 分布内适应 |
| unseen SNR | 15/5 | independent/electrode | train combos | 强度泛化 |
| extreme | 0 | independent/electrode | train combos | 压力测试 |
| unseen combo | 10 | electrode | held-out combos | 电极组合泛化 |
| joint unseen | 15/5 | electrode | held-out combos | 最重要泛化组 |
| covariance control | 20/10/15/5/0 | covariance | all/held-out target | 机制消融 |
| NSTDB sensitivity | 20/10/0 | bw/ma/em | 预设传播方式 | 探索性外部噪声回放 |

### 主终点

建议预先指定两个主终点：

1. `Macro-AUROC` 在 **unseen SNR + unseen electrode combination** 测试组上的下降量；
2. 相对于 clean-only 的 Macro-AUROC 改善量：

\[
\Delta M_{strategy}
=M_{strategy}-M_{clean-only}.
\]

其中，主比较为：

- electrode augmentation vs clean-only；
- electrode augmentation vs independent-noise augmentation；
- mixed augmentation vs electrode augmentation。

不要把所有测试条件和所有指标都视为同等 confirmatory endpoint。

---

## 9. 评价指标

### 9.1 分类性能

主指标：

- Macro-AUROC；
- Macro-AP。

辅助指标：

- Macro-F1；
- 每类 AUROC、AP、F1；
- Micro-AUROC 和 Micro-F1。

### 9.2 鲁棒性指标

对每个策略计算：

\[
Drop=M_{clean}-M_{noisy}
\]

以及：

\[
Retention=\frac{M_{noisy}}{M_{clean}+10^{-12}}.
\]

同时报告 clean 性能代价：

\[
CleanCost=M_{clean-only,clean}-M_{strategy,clean}.
\]

一个训练策略不能只因为 noisy AUROC 较高就被判定为更好。如果它显著损害 clean AUROC，也需要报告这个代价。

### 9.3 校准和固定阈值性能

保留第一阶段指标：

- Brier score；
- ECE；
- 固定 clean-validation 阈值下的 Macro-F1；
- clean/noisy 预测一致率；
- 平均概率变化；
- sensitivity、specificity、PPV、NPV 作为补充。

---

## 10. 统计分析方案

### 10.1 训练种子层面

每个策略×模型×测试条件至少有 3 个训练种子，推荐 5 个。报告：

- 均值；
- 样本标准差；
- 训练种子 t 区间；
- 每个 seed 的原始结果。

不要把训练种子数量当作患者数量。

### 10.2 患者簇 bootstrap

对每个固定 checkpoint，在 test patient_id 层面进行 bootstrap：

- 抽样单位为患者；
- 保留患者的全部 ECG；
- 使用相同 bootstrap 抽样索引比较所有策略；
- 至少 2000 次 replicate；
- 使用预先固定 bootstrap seed；
- 计算每个策略相对于 clean-only 和 independent augmentation 的配对差；
- 报告 95% percentile CI。

### 10.3 主要比较

主比较限定为：

1. electrode vs clean-only；
2. electrode vs independent-rms；
3. mixed vs electrode。

每个主终点分别进行配对比较。多重比较使用 Holm 校正，或者提前规定只对一个主要联合终点进行确认性检验，其余作为探索性分析。

### 10.4 效应量

除了 p 值，必须报告：

- AUROC 绝对差；
- 相对 clean 的保留率差；
- 患者级 bootstrap CI；
- Cohen’s dz 或配对效应量；
- clean 性能代价。

如果 CI 包含 0，不得写“无效”，应写“当前样本和实验条件下未观察到稳定差异”。

### 10.5 策略排序

对四种策略在每个测试组的排名使用：

- 平均排名；
- 每个训练 seed 的排名；
- Kendall 或 Spearman 作为描述性指标。

不要因为某一个 SNR 下排名改变就宣称存在普遍训练策略排序反转。

---

## 11. 最小实验矩阵与运行顺序

### 阶段 A：小规模 pilot

使用第一阶段 pilot 子集：

- train 4000；
- validation 1000；
- test 1000；
- 每策略×模型 1 个 seed；
- 10 epochs；
- 测试 clean、10 dB、5 dB、0 dB；
- 测试 2 个未见电极组合。

目标是验证：

- 四种策略能够训练；
- 增强比例符合配置；
- 训练 SNR 和测试 SNR 不泄漏；
- 未见组合确实未出现在训练配置；
- 指标和 checkpoint 保存正确。

### 阶段 B：主 full 训练

- 全部 17084 条训练记录；
- clean validation 2146 条；
- test 2158 条；
- 2 个模型；
- 4 个策略；
- 3 或 5 个训练种子；
- 25 epochs；
- 训练 SNR 20/10 dB；
- 测试 SNR 20/15/10/5/0 dB；
- 主测试包括 unseen combo。

### 阶段 C：固定 checkpoint 评估

训练完成后，所有测试输入一次性固定生成并缓存。然后用不同策略 checkpoint 做前向评估，确保比较时输入完全相同。

### 阶段 D：统计和报告

先生成原始逐记录预测，再生成：

- seed summary；
- patient bootstrap；
- clean cost；
- unseen SNR 曲线；
- unseen electrode combination 热图；
- 策略排名图；
- calibration 图。

不得先看完整测试汇总再修改主实验配置。

---

## 12. 预期结果及解释规则

### 结果类型一：Electrode augmentation 在未见组合和未见 SNR 上最好

可以支持：

> 采集机制一致的电极空间增强能够改善对未知结构化扰动的泛化。

但仍需检查 clean cost 是否可接受。

### 结果类型二：Electrode augmentation 只在 seen 条件好

说明模型可能只是适应训练分布，不能称为泛化改进。应继续检查 held-out combination 和 15/5 dB。

### 结果类型三：Independent augmentation 和 electrode augmentation 接近

说明当前任务或模型对结构差异不敏感，或训练中的结构信息没有转化为泛化优势。这并不推翻第一阶段的评价差异结论，但会削弱“结构化增强优于传统增强”的训练结论。

### 结果类型四：Mixed augmentation 最好

需要确认优势是否来自：

- 覆盖两类扰动机制；
- 更丰富的输入分布；
- clean 代价较低；
- 对未见条件更稳定。

不能只因 mixed 的平均值最大就认定其最优，还需报告不同 seed 和不同测试组的一致性。

### 结果类型五：所有策略在 0 dB 都失败

这可能是极端 SNR 下信息已经被噪声覆盖，不应强行解释为训练失败。应重点比较 15/10/5 dB 的相对保留率，并说明 0 dB 是压力测试而非唯一主终点。

---

## 13. 关键配置示例

### 13.1 四策略配置

```yaml
phase: phase2
seed: 17
model: resnet
max_epochs: 25
batch_size: 128
learning_rate: 0.001
weight_decay: 0.0001
sampling_rate: 100
sequence_length: 1000

train:
  snr_db: [20, 10]
  snr_probability: [0.5, 0.5]
  clean_probability: 0.5
  augmentation_probability: 0.5
  noise_seed_range: [10000, 19999]
  electrode_combo_file: configs/phase2_train_electrode_combos.json

strategies:
  clean_only:
    clean_probability: 1.0
    independent_probability: 0.0
    electrode_probability: 0.0
  independent_rms:
    clean_probability: 0.5
    independent_probability: 0.5
    electrode_probability: 0.0
  electrode:
    clean_probability: 0.5
    independent_probability: 0.0
    electrode_probability: 0.5
  mixed:
    clean_probability: 0.5
    independent_probability: 0.25
    electrode_probability: 0.25

test:
  snr_db: [20, 15, 10, 5, 0]
  noise_seed_range: [20000, 29999]
  use_same_inputs_across_strategies: true
  clean_threshold_source: validation_clean
  unseen_combo_file: configs/phase2_test_electrode_combos.json

statistics:
  patient_bootstrap_replicates: 2000
  bootstrap_seed: 20260919
  unit_of_resampling: patient_id
  multiple_testing: holm
```

### 13.2 训练电极组合

```json
[
  ["RA", "LA", "LL"],
  ["V1"],
  ["V2"],
  ["V3"],
  ["V4"],
  ["V5"],
  ["V6"],
  ["RA", "LA"],
  ["LA", "LL"],
  ["RA", "LL"]
]
```

### 13.3 测试电极组合

```json
[
  ["RA"],
  ["LA"],
  ["LL"],
  ["RA", "V1"],
  ["LA", "V6"],
  ["LL", "V4"],
  ["RA", "LA", "V1", "V6"],
  ["RA", "V2", "V5"],
  ["LA", "V3", "V6"],
  ["LL", "V1", "V4"]
]
```

在正式运行前必须用程序自动检查：

```text
set(train_combo) ∩ set(test_combo) == empty
```

并检查测试组合没有通过训练策略中的“random 2–4”隐式出现。主实验不允许使用未枚举的随机组合。

---

## 14. 交付物

第二阶段完成后，必须至少交付：

```text
phase2/
├── configs/
│   ├── phase2_main.yaml
│   ├── phase2_train_electrode_combos.json
│   └── phase2_test_electrode_combos.json
├── src/
│   ├── train_phase2.py
│   ├── generate_phase2_noise.py
│   ├── evaluate_phase2.py
│   └── statistics_phase2.py
├── results/
│   ├── checkpoints/
│   ├── predictions/
│   ├── tables/
│   ├── figures/
│   └── logs/
├── reports/
│   └── phase2_final_report.md
└── README.md
```

至少应包含：

1. 四种策略的 clean 性能；
2. 见过和未见 SNR 的性能；
3. 见过和未见电极组合的性能；
4. 0 dB 压力测试；
5. Macro-AUROC、Macro-AP、Macro-F1、Brier、ECE；
6. clean cost；
7. 患者 bootstrap CI；
8. 每个训练 seed 的原始结果；
9. 训练噪声和测试噪声的 seed 隔离证据；
10. 训练/测试电极组合不重叠的自动检查结果。

---

## 15. 第二阶段的主要判断标准

最终不要只问“哪个策略平均分最高”，而要按以下顺序判断：

1. 是否提高 clean-only baseline 在未见 SNR 上的鲁棒性？
2. 是否提高对未见电极组合的泛化？
3. 是否在两个模型和多个训练 seed 上方向一致？
4. 是否保持可接受的 clean 性能？
5. 是否只对合成 Gaussian 有效，还是对 NSTDB 至少显示相同方向？
6. 是否存在明显的模型依赖性？
7. 是否需要把结论限定为评价协议改进，而不是训练方法普遍优越？

推荐把第二阶段主终点写成：

> 在未见 SNR 和未见电极组合的联合测试条件下，electrode augmentation 相对于 clean-only 和 independent-rms augmentation 的 Macro-AUROC 保留率差异。

如果这一主终点在患者 bootstrap 和多个 seed 下得到稳定支持，才可以把“采集机制一致训练”列为论文的第二个主要贡献。

---

## 16. 不得违反的规则

1. 不得在测试集上选择增强概率、SNR 或电极组合。
2. 不得为不同策略单独调学习率或 epoch。
3. 不得使用 noisy validation 选择主 checkpoint。
4. 不得把训练噪声 seed 和测试噪声 seed 混用。
5. 不得把 0 dB 结果作为唯一主结论。
6. 不得把 NSTDB 结果当作纯跨导联协方差因果证据。
7. 不得在看过 full test 结果后更改 mixed 比例。
8. 不得把 clean 性能下降隐藏起来。
9. 不得用测试集重新选择分类阈值。
10. 不得用单一训练 seed 的结果决定哪种策略最优。

**第二阶段的目标不是制造一个必然胜出的 electrode augmentation，而是严谨检验采集机制一致扰动是否具有未知条件泛化价值。**
