# 面向方法学与信号处理投稿的补充实验方案

## 一、总体判断

目前两阶段实验已经形成完整故事，但如果投稿到 Biomedical Signal Processing、MLSP、ICASSP 等偏方法学和信号处理方向，评审最可能追问的不是“有没有更多模型”，而是以下四个问题：

1. 观察到的性能差异是否确实来自跨导联结构，而不是噪声能量、频谱、幅度或时序差异？
2. 矩阵 \(A\) 是否具有足够的信号处理解释，而不是任意相关噪声生成器？
3. 结构效应是否依赖于特定 SNR、特定电极组合、特定数据集或特定模型？
4. 电极空间增强带来的收益是否超过普通增强的计算和 clean 性能代价？

因此，投稿前不建议继续无目的增加大量模型或噪声类型。应优先补足**因果隔离、结构解释、边界分析和可视化证据**。

---

## 二、补充实验优先级

| 优先级 | 内容 | 目的 | 建议状态 |
|---|---|---|---|
| A | 严格结构隔离消融 | 证明差异不是 RMS、PSD 或边际幅度造成 | 必须补或明确已有结果 |
| A | 矩阵 A 与电极组合消融 | 证明结果由采集结构决定且可解释 | 必须补 |
| A | SNR×结构效应曲线 | 明确结论的适用范围 | 必须补图，最好补统计摘要 |
| A | 训练收益与评价效应分离 | 防止把第一、二阶段两个命题混为一谈 | 必须补分析图 |
| B | 信号处理参数敏感性 | 说明结果不依赖某个 FFT/Welch 实现 | 建议补 |
| B | 模型和输入导联敏感性 | 说明现象不是单一架构或导联设置产物 | 建议补 |
| B | 500 Hz/外部数据敏感性 | 提高投稿可信度和外部有效性 | 有资源再补 |
| C | 更多复杂模型、更多噪声种类 | 扩展普适性 | 不建议作为首要补实验 |

---

# 三、优先级 A：必须补强的消融实验

## A1. 严格区分“协方差效应”和“时序/频谱效应”

### 研究问题

电极映射噪声造成的性能下降，究竟主要来自：

- 跨导联零时滞协方差；
- 跨导联时滞和相位关系；
- 单导联频谱差异；
- 高阶幅度分布差异；
- 电极空间生成路径本身。

### 当前已有证据

已有 covariance-matched 和 marginal-shift 实验已经非常有价值，但报告也明确承认：

- covariance matching 没有完全匹配逐记录 Welch PSD；
- marginal shift 保留周期谱和逐导联经验边际，却改变了跨导联时滞关系；
- 高阶联合分布仍未完全隔离。

因此建议新增一个**四条件结构消融矩阵**，至少在 10 dB 和 0 dB、ResNet 和 TCN 上执行：

| 条件 | 单导联边际 | 零时滞协方差 | 跨导联时滞 | 生成空间 |
|---|---:|---:|---:|---|
| Independent-RMS | 近似匹配 | 否 | 否 | 导联 |
| Electrode-mapped | 不完全相同 | 是 | 是 | 电极 |
| Covariance-matched | 近似匹配 | 是 | 视生成器而定 | 导联 |
| Marginal-shift | 严格匹配 | 被削弱 | 保留部分 | 导联/移位 |

### 推荐新增的严格版本

如果计算资源允许，构造一个**频域协方差匹配**条件：

\[
S_n(f)=A S_\epsilon(f)A^T
\]

在每个频率 bin 上匹配多元谱矩阵，而不是只匹配时域协方差。这样可以生成：

\[
n_{freq}(f)=L(f)z(f),
\quad L(f)L(f)^H=S_n(f).
\]

该条件可用于检验：

- 电极映射与导联空间频率协方差匹配是否产生近似结果；
- 性能差异是否来自频率相关的跨导联结构，而不只是零时滞协方差。

不必把它作为大规模训练实验，建议只用于测试端的机制消融。

### 主指标

- Macro-AUROC；
- Macro-F1；
- 性能下降量；
- covariance-matched 与 electrode-mapped 的差值；
- 每频率的谱矩阵误差。

### 推荐图

1. 四条件 12×12 相关矩阵；
2. 四条件每频率 cross-spectral coherence 热图；
3. 四条件 Macro-AUROC 下降量；
4. 条件之间的效应量森林图。

### 结论边界

如果新增频域协方差匹配后结果接近 electrode-mapped，可以加强：

> 跨导联的频率相关结构可能是主要解释因素。

如果结果仍有差异，只能说明：

> 除二阶频率结构外，电极生成过程还伴随未匹配的时间、高阶或非平稳结构。

这两种结果都具有方法学价值。

---

## A2. 电极映射矩阵 A 的结构消融

### 研究问题

结果是否依赖于标准 12 导联映射的具体结构？

### 条件设计

构造以下矩阵或扰动传播条件：

| 条件 | 定义 | 作用 |
|---|---|---|
| Standard A | 当前标准 WCT/Einthoven 矩阵 | 主方法 |
| A-no-WCT | 胸前导联不共享肢体电极 | 检验 WCT 共享结构作用 |
| A-limb-only | 只扰动 RA/LA/LL | 检验肢体导联传播 |
| A-precordial-only | 只扰动 V1–V6 | 检验胸前导联局部传播 |
| A-randomized | 保持矩阵稀疏度/列范数，但随机打乱连接 | 结构负对照 |
| A-diagonal-like | 没有共享电极的近似独立映射 | 接近独立导联基线 |

主实验不需要所有条件都训练。建议测试端先做：

- Standard A；
- A-limb-only；
- A-precordial-only；
- A-randomized。

### 必须报告的结构量

对每个矩阵计算：

- 矩阵 rank；
- nullity；
- 每列非零数；
- 每个电极影响的导联数量；
- 每个导联包含的电极数量；
- \(AA^T\) 的非对角绝对值均值；
- \(A^TA\) 的列间相似度。

### 推荐图

1. 不同 A 的矩阵热图；
2. 电极—导联二部图；
3. 单位电极扰动传播图；
4. `effective cross-lead correlation` 与模型性能下降的散点图；
5. A 的奇异值谱。

### 预期价值

如果 Standard A 的性能效应强于随机连接矩阵，说明结果与具体采集结构相关，而不是“任意相关噪声都会造成同样结果”。

---

## A3. SNR × 噪声结构效应曲线

### 研究问题

结构化噪声效应是否只在 0 dB 极强噪声下出现？

### 建议分析

将第一、二阶段结果合并成结构效应曲线：

\[
E_{structure}(s)
= M_{electrode}(s)-M_{independent\_rms}(s).
\]

至少报告：

- 20 dB；
- 15 dB；
- 10 dB；
- 5 dB；
- 0 dB。

分别绘制：

- 第一阶段 clean-only 模型的评价效应；
- 第二阶段四种训练策略的测试效应；
- ResNet 与 TCN 分面图。

### 推荐图

1. 横轴 SNR，纵轴 \(\Delta AUROC\)；
2. 横轴 SNR，纵轴 \(\Delta F1\)；
3. 横轴 SNR，纵轴 calibration error 差；
4. 每个点显示训练 seed SD 和患者 bootstrap CI。

### 关键解释

如果曲线表现为：

- 20 dB 接近零；
- 10 dB 较小且模型依赖；
- 5/0 dB 明显；

应把它写成：

> 结构效应具有扰动强度依赖性，而非在所有噪声水平下均匀存在。

这比声称普遍偏差更可信。

---

## A4. “评价效应”和“训练效应”分离图

### 研究问题

第一阶段证明了不同测试噪声协议会改变结果；第二阶段研究了训练增强是否改变泛化。必须把两者明确分离。

### 建议构造二维图

横轴：测试条件下的结构效应：

\[
E_{eval}=M_{electrode}-M_{independent\_rms}
\]

纵轴：训练策略带来的泛化收益：

\[
E_{train}=M_{strategy}-M_{clean\_only}
\]

每个点对应：

- 模型；
- SNR；
- 电极组合；
- 测试噪声类型。

### 解释

这张图可以显示：

- 结构效应强，但训练收益弱；
- 结构效应强且训练收益强；
- 结构效应弱但训练收益存在；
- 评价和训练不一定是同一个问题。

这是论文中最有方法学价值的图之一。

---

# 四、优先级 B：建议补充的信号处理消融

## B1. 频谱和滤波参数敏感性

当前主合成噪声为 0.5–40 Hz、100 Hz 采样率的带限 Gaussian。建议至少补一个频带消融：

| 噪声频带 | 目的 |
|---|---|
| 0.05–5 Hz | 基线漂移主导 |
| 5–15 Hz | ECG 形态/低频中段 |
| 15–40 Hz | 肌电/高频扰动 |
| 0.5–40 Hz | 当前主条件 |
| 0.5–49 Hz | 更宽带压力 |

建议只在测试端做，不必重新训练四种策略。使用第一阶段六个 checkpoint 或第二阶段 checkpoint 的代表性子集即可。

### 推荐图

- 频带 × 结构条件的性能下降热图；
- 每个频带的结构效应森林图；
- 结构效应与频带功率占比的散点图。

### 价值

这能回答结构效应是否主要由某个频带触发，避免把带限 Gaussian 的特定频谱误当作普遍跨导联机制。

---

## B2. 采样率和重采样敏感性

如果投稿信号处理方向，100 Hz 是一个需要主动承认的限制。资源允许时做：

1. PTB-XL 500 Hz 输入；
2. 500 Hz 下生成噪声，再统一下采样到 100 Hz；
3. 100 Hz 直接生成噪声作为当前基线。

不必完整重跑 40 个模型。推荐：

- ResNet 和 TCN 各选择 clean-only、independent-RMS、electrode 三种训练策略；
- 3 个 seed；
- 10 dB 和 0 dB；
- 测试全电极和 2 个未见组合。

如果 500 Hz 与 100 Hz 方向一致，外部可信度会明显提高；如果不一致，应讨论高频结构与采样率的交互。

---

## B3. 电极组合和传播范围消融

当前单电极结果可以进一步转换成更清晰的 signal-processing 分析。

对每个电极计算：

\[
C_k=\|A_{:,k}\|_2
\]

以及：

\[
R_k=\#\{i:A_{i,k}\ne0\}.
\]

再比较：

- 矩阵传播强度 \(C_k\)；
- 实际噪声 RMS；
- 模型性能下降；
- 梯度敏感性或输入遮挡敏感性。

### 推荐图

- 电极传播强度 vs AUROC 下降散点图；
- RA、LA、LL、V1–V6 的排序条形图；
- 每个电极对 12 导联的传播热图；
- 模型间敏感性差异图。

### 注意

不能把这个结果解释为临床电极脱落风险排序，因为当前实验的全局 SNR 标定会造成不同电极的局部噪声强度不同。

---

## B4. 导联子集消融

为了判断结构效应是由肢体导联还是胸前导联主导，可测试：

| 输入导联 | 目的 |
|---|---|
| 全 12 导联 | 主结果 |
| 仅肢体 6 导联 | 肢体共享电极结构 |
| 仅胸前 V1–V6 | WCT 共享结构 |
| 仅 I、II、V1、V6 | 低维代表性输入 |
| 随机去除一个导联 | 检验模型是否依赖冗余结构 |

建议只做 clean-only 和 electrode 测试，不需要重新训练所有策略。若模型必须重新训练，应固定训练预算并明确这是独立子实验。

### 推荐图

- 导联子集 × SNR 的性能热图；
- 子集间结构效应差值；
- 输入导联数与结构效应的关系。

---

## 五、优先级 B：方法学训练分析

## C1. 增强概率消融

当前主配置为 50% clean、50% 加噪。建议测试端或小规模训练端增加：

- 25% augmentation；
- 50% augmentation；
- 75% augmentation；
- 100% augmentation。

只在 electrode augmentation 上做即可。主指标：

- clean AUROC；
- joint-unseen retention；
- clean cost；
- calibration。

### 推荐图

横轴增强概率，纵轴分别画：

- clean performance；
- noisy performance；
- retention；
- clean cost。

这能说明电极增强不是因为“更多噪声暴露”而获得偶然收益，也能帮助解释 mixed 不占优的原因。

## C2. 训练 SNR 分布消融

比较：

- 只用 20 dB；
- 只用 10 dB；
- 20/10 各半；
- 20/10/0 各三分之一。

建议只对 electrode 和 independent 两种策略做小规模训练，不能在看 full 结果后把最优 SNR 配置升级为主结果。

主问题是：

> 训练中是否需要包含极端 0 dB 才能提升对 5 dB 或 0 dB 的泛化？

## C3. 训练组合覆盖率消融

比较：

- 只扰动全部肢体电极；
- 只扰动单胸前电极；
- 肢体+胸前混合；
- 预定义多组合训练。

这样可以回答：

> 泛化收益来自“电极空间”本身，还是来自训练中覆盖了足够多的电极组合？

---

## 六、优先级 C：外部有效性补充

## D1. NSTDB 不要再作为主因果证据

NSTDB 可以保留为敏感性分析，但不建议继续围绕它增加大量复杂解释。报告应持续强调：

- 真实采集噪声源存在；
- 但传播方式、PSD、时序和结构均存在混杂；
- 因此只能作为方向敏感性检查。

## D2. 外部数据集

资源允许时优先选择一个外部 12 导联数据集，而不是再增加多个 synthetic noise 类型。可考虑：

- PhysioNet/CinC Challenge 2021 数据；
- 或一个许可明确、标签任务可以映射到相近类别的临床 12 导联数据集。

外部验证不必完整重跑第二阶段。推荐：

- 使用第一阶段或第二阶段已训练 checkpoint；
- 只做 clean、10 dB、0 dB；
- independent-RMS vs electrode；
- 1–2 个模型；
- 报告方向和效应量，不强行做复杂显著性结论。

---

# 七、推荐的最终投稿图表结构

## 主文建议保留 6 张核心图

### 图 1：采集结构与扰动生成机制

内容：

```text
电极节点 -> A -> 12 导联
```

同时展示：

- 标准矩阵热图；
- 单电极传播；
- independent 与 electrode 噪声波形。

### 图 2：三类噪声统计结构对比

包含：

- 协方差矩阵；
- 相关矩阵；
- PSD；
- cross-spectral coherence。

### 图 3：第一阶段 SNR-结构效应曲线

展示 ResNet/TCN 在 20/15/10/5/0 dB 的：

\[
AUROC_{electrode}-AUROC_{independent\_rms}
\]

并显示 seed SD 和患者 bootstrap CI。

### 图 4：第二阶段四策略联合未见泛化

用 forest plot 或 dot-and-whisker plot 展示：

- electrode − clean-only；
- electrode − independent-RMS；
- mixed − electrode。

分别显示 ResNet 和 TCN。

### 图 5：未见电极组合热图

横轴：10 个 held-out electrode combinations。

纵轴：四种训练策略或三种模型。

颜色：

- AUROC；
- retention；
- 相对于 clean-only 的差值。

图中标注训练 seed 变异范围。

### 图 6：clean cost–robustness benefit Pareto 图

横轴：clean AUROC cost。

纵轴：joint-unseen retention gain。

每个点代表一个训练策略×模型。右上方代表更好，但必须同时显示误差条。

## 补充材料建议

- 完整 20/15/10/5/0 dB 表；
- NSTDB 全部敏感性结果；
- 每电极传播和单电极结果；
- 全部训练 seed；
- 500 Hz 或频带敏感性；
- 统计 bootstrap 细节；
- 失败和修复日志可以留在项目归档，不必全部放正文。

---

# 八、统计和投稿纪律

1. 补实验必须在运行前固定配置和主要终点。
2. 不要根据现有结果挑选最有利的 SNR、电极组合或模型。
3. 新增消融必须标记为 confirmatory 或 exploratory。
4. 补实验数量增加后，重新定义多重比较家族，或明确只作描述性分析。
5. 所有点估计同时报告 seed SD 或 bootstrap CI。
6. 不要用 patient bootstrap CI 替代训练 seed 不确定性。
7. 不要把“区间跨零”写成“证明没有效应”。
8. 不要把“点估计为正”写成“统计学显著”。
9. 不要把更多图等同于更多证据；每张图都要回答一个明确问题。
10. 不要把 NSTDB 混杂结果和 Gaussian 结构控制结果放在同一证据层级。

---

# 九、最小可行补充包

如果时间和算力有限，建议只完成以下五项：

1. **A 矩阵结构消融**：Standard A、A-limb-only、A-precordial-only、A-randomized。
2. **SNR×结构效应曲线**：20/15/10/5/0 dB，两个模型。
3. **频带敏感性**：0.05–5、5–15、15–40、0.5–40 Hz。
4. **评价效应—训练效应二维图**。
5. **clean cost–joint-unseen benefit Pareto 图**。

这五项比再增加几个复杂模型更能提高方法学论文的说服力。

---

# 十、最终建议

如果只能选择一个新增实验，优先做：

> **频率相关跨导联协方差匹配 + Standard A / randomized A 对照。**

如果只能选择一个新增可视化，优先做：

> **SNR×结构效应曲线，并同时显示第一阶段评价差异和第二阶段训练收益。**

如果只能选择一个外部验证，优先做：

> **500 Hz 或一个外部 12 导联 ECG 数据集上的 independent-RMS vs electrode 测试复现。**

论文不需要证明电极增强在所有情况下都最优。方法学投稿真正需要证明的是：

1. 结构化扰动在数学和信号处理上可解释；
2. 它改变了传统鲁棒性评价结果；
3. 这种影响具有可重复的边界和机制；
4. 它在部分未见条件下可以指导训练；
5. 结论经过充分的负对照和敏感性分析后仍然成立。

目前的两阶段结果已经具备这条主线，补充实验的任务是加强因果隔离和边界解释，而不是制造一个绝对最优的训练策略。
