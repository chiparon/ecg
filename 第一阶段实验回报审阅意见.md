# 第一阶段实验回报审阅意见

## 一、总体判断

第一阶段已经完成了从数据管线、导联映射、噪声生成、模型训练到统计复核的完整闭环，工作量和可复现性明显超过普通 pilot。当前结果已经足以支持一个**有范围限定的阶段性结论**：

> 在 PTB-XL 多标签分类任务、当前使用的两种模型、合成带限 Gaussian 噪声和 0 dB 强扰动条件下，具有跨导联结构的电极映射噪声会造成比独立导联噪声更大的性能下降。因此，单纯使用独立导联噪声可能高估模型对这类结构化采集扰动的鲁棒性。

这个结论具有研究价值，但目前还不能表述为“独立噪声评价在 ECG 鲁棒性研究中普遍失效”，也不能表述为“电极映射噪声已经等价于真实临床采集噪声”。现有实验最强的证据集中在 **0 dB 合成带限 Gaussian 噪声**，而 20 dB 差异接近零、NSTDB 存在 PSD 混杂，因此需要保持谨慎。

---

## 二、结果中最有说服力的部分

### 1. 结果具有模型跨架构一致性

在 0 dB 条件下，电极映射噪声相对于 RMS 匹配独立噪声的 Macro-AUROC 差异为：

- ResNet：-0.02075，训练种子间 95% CI 为 [-0.02751, -0.01400]；
- TCN：-0.01269，训练种子间 95% CI 为 [-0.02285, -0.00253]。

两个模型的差异方向一致，说明现象不太可能只是单一架构的偶然结果。TCN 的差异较小但仍为负，这使“跨导联结构可能影响鲁棒性评价”的论点更稳健。

### 2. RMS 匹配对照增强了因果解释

独立导联噪声与电极映射噪声不是简单的能量对比。报告同时给出了 RMS 匹配独立噪声，并且 ResNet 从 0.83601 下降到 0.81526，TCN 从 0.89111 下降到 0.87842。这说明观察到的差异不能直接归因于独立噪声总体幅度更小或更大。

### 3. 协方差匹配结果接近电极映射结果

协方差匹配条件在两个模型上的结果分别为：

- ResNet：0.81435；电极映射：0.81526；
- TCN：0.87751；电极映射：0.87842。

这一现象支持一个重要解释：**最终导联空间的跨导联协方差结构可能是性能差异的主要因素之一。** 但当前结果只能说“接近”或“与该解释一致”，不能据此宣称统计等价。还需要报告两种噪声的协方差 Frobenius 相对误差、逐导联方差误差和相关矩阵误差。

### 4. 多噪声重放提高了结果可信度

追加五组独立噪声重放后，两种模型的 15/15 个训练种子×噪声组合在 0 dB 下均为负差，这是比单次噪声实现更有价值的稳定性证据。建议在最终论文中将这部分结果明确列为“noise realization robustness analysis”。

---

## 三、必须在论文或最终报告中修正的口径

### 1. CI 的含义必须写清楚

当前报告已经说明 CI 是训练种子间 CI，而不是患者 bootstrap CI。这一点必须保留，并且不能把它写成“95% 置信区间证明患者层面显著”。

建议同时补充两类区间：

1. **训练种子层面**：均值±标准差，或基于 3/多个训练种子的区间；
2. **患者/记录层面**：在固定模型和固定噪声配对条件下，对测试记录进行 patient-level bootstrap，报告 AUROC 差异的 95% bootstrap CI。

如果患者级 bootstrap 不方便，可以先做 record-level paired bootstrap，但报告必须说明 PTB-XL 中同一患者可能有多条 ECG，严格意义上的独立重采样单位应优先使用 patient_id。

### 2. “完整官方划分”的样本数量需要解释

回报中的 full 数量为：

- train：17084；
- validation：2146；
- test：2158。

三者合计为 21388，而官方 PTB-XL 1.0.3 页面说明数据集约有 21799 条记录。最终报告必须说明少掉的记录是什么，例如：

- 是否因为没有可用 diagnostic superclass 标签；
- 是否因为预处理失败；
- 是否因为重复记录或质量过滤；
- 是否因为只保留满足某个标签条件的记录。

不能将 17084/2146/2158 直接写成“完整数据集划分”，除非报告已经给出被排除记录的数量、原因和清单。

### 3. 采样率口径必须明确

当前回报没有说明 full 主实验使用 PTB-XL 的 100 Hz 还是 500 Hz 版本。最终报告应明确：

- 主实验采样率；
- 是否进行了 500 Hz 敏感性实验；
- NSTDB 噪声是否经过重采样；
- 重采样方法和抗混叠滤波设置。

如果 full 使用 100 Hz，不能在表述中暗示已经验证了全部高频采集噪声行为。

### 4. WCT 矩阵修正必须同步到所有文件

报告记录已将胸前导联的肢体电极系数从 `+1/3` 修正为 `-1/3`。按照

\[
V_i = V_{chest,i} - (V_{RA}+V_{LA}+V_{LL})/3
\]

的 Wilson central terminal 约定，胸前导联中 RA、LA、LL 的系数应为 `-1/3`，对应胸前电极系数为 `+1`。必须确认以下内容全部使用修正后的矩阵：

- 源码；
- 生成的噪声；
- 已保存的预测结果；
- 图；
- 中期报告和最终报告；
- 配置或缓存中的矩阵哈希。

如果早期实验使用了错误矩阵，必须说明哪些结果重算，哪些结果仅为旧归档，不能混用。

---

## 四、当前结论应如何写

建议采用以下表述：

> 在 PTB-XL 的多标签 ECG 分类实验中，采用两个不同的一维时序模型，并在相同目标 SNR 和近似匹配的逐导联 RMS 条件下进行测试。结果显示，在 0 dB 合成带限 Gaussian 噪声下，电极映射噪声导致的 Macro-AUROC 下降 consistently larger than independent lead noise。协方差匹配导联噪声与电极映射噪声的结果接近，提示跨导联协方差结构可能是影响模型鲁棒性的关键因素之一。该证据支持重新审视独立导联噪声作为多导联 ECG 鲁棒性评价基线的充分性，但结论目前限定于当前任务、模型、噪声类型和 SNR 范围，不能直接推广到所有临床采集扰动。

中文可以写为：

> 在当前实验范围内，跨导联相关结构会影响多导联 ECG 模型的鲁棒性评价。独立导联噪声在强合成扰动条件下可能高估模型性能，但这一结论尚需在更丰富的 SNR、真实采集噪声回放和外部数据集上进一步验证。

应避免：

- “证明了独立噪声评价存在普遍偏差”；
- “电极映射噪声等价于真实临床噪声”；
- “协方差匹配与电极映射在统计上完全等价”；
- “结果适用于所有 ECG 模型”。

---

## 五、进入第二阶段前的优先补实验

### 优先级 A：患者级 bootstrap

固定已经训练好的 checkpoint 和同一批测试样本，对每个 patient_id 进行 bootstrap，计算：

\[
\Delta AUROC = AUROC_{electrode}-AUROC_{RMS-independent}.
\]

至少报告：

- 每个模型的 patient-level bootstrap 95% CI；
- bootstrap 分布图；
- 两种噪声条件下的每类别差异；
- bootstrap 重复次数和随机种子。

这是当前最重要的统计补强，因为现有区间主要反映训练种子不确定性。

### 优先级 B：明确协方差匹配程度

生成一张表，至少包含：

| 指标 | Electrode-mapped vs Covariance-matched |
|---|---|
| 协方差 Frobenius 相对误差 | 数值 |
| 相关矩阵 MAE | 数值 |
| 逐导联方差最大相对误差 | 数值 |
| 实际 SNR 差异 | 数值 |
| PSD 频带误差 | 数值 |

这样才能判断“协方差匹配结果接近”是理论机制，还是仅仅数值偶合。

### 优先级 C：补齐 20 dB、10 dB 的主结果表

当前摘要只给出了 0 dB 的完整表，但报告已经说明 20 dB 差异接近零、10 dB 尤其 TCN 对噪声实现敏感。最终报告必须给出 20/10/0 dB 的完整结果，而不是只展示最有利于论点的 0 dB。

建议同时给出：

- 绝对 AUROC；
- 相对 clean 的性能下降；
- 训练种子均值±SD；
- noise replay 的均值±SD；
- patient-level bootstrap CI（完成后）。

### 优先级 D：把 NSTDB 分析降级为混杂敏感性实验

由于 NSTDB 三类噪声带来明显 PSD 混杂，不能将 NSTDB 结果直接与合成带限 Gaussian 主结果放在同一因果层级。正确写法是：

> NSTDB 回放用于检验结论在实际采集噪声片段下的方向稳定性，但由于不同噪声类型具有不同频谱，结果同时受到噪声频谱和跨导联结构的共同影响。

如果要在 NSTDB 上做更强因果比较，应进一步对不同条件做频带匹配或选择同一噪声源进行 independent/electrode 两种传播方式。

### 优先级 E：增加一个“只改变协方差，不改变边际统计”的严格版本

主线中已有 RMS 匹配和 covariance-matched 条件，但最好确认 `Independent` 与 `Electrode-mapped` 在以下方面同时接近：

- 每个 lead 的方差；
- 每个 lead 的 PSD；
- 每条记录的全局 SNR；
- 每个频带的功率。

若只能实现其中一部分，报告应明确这是“近似公平控制”，而非完全匹配。

---

## 六、第一阶段是否可以结束？

**可以结束第一阶段的主体开发，但不建议立即把当前版本作为论文最终统计结论。**

当前状态适合定义为：

> Phase 1 completed: reproducible mechanism and robustness pilot/full evaluation completed; final inference requires patient-level bootstrap and explicit sample exclusion accounting.

也就是说，代码、模型、噪声机制和主现象已经达到“第一阶段完成”的标准；在进入第二阶段鲁棒训练之前，补完患者级 bootstrap、样本数量口径、矩阵版本一致性和 20/10/0 dB 全结果表即可。

---

## 七、第二阶段建议的研究主线

第二阶段不要直接把所有噪声混合到训练中。建议采用最小的训练实验矩阵：

| 训练策略 | 测试条件 |
|---|---|
| Clean baseline | clean、independent、electrode、covariance-matched |
| Independent augmentation | 同上 |
| Electrode-mapped augmentation | 同上 |
| Mixed augmentation | 同上 |

测试时加入未见 SNR 和未见电极组合。例如训练使用 20/10 dB 和固定电极组合，测试加入 15/5 dB 以及未见的单电极或多电极组合。核心指标仍应使用 Macro-AUROC、Macro-F1、性能下降量和 patient-level bootstrap CI。

第二阶段的主要问题应改写为：

> 采集机制一致的训练扰动是否能提高对未见电极组合和未见噪声强度的泛化，而不是只提高对训练时相同噪声的适应？

---

## 八、最终审阅结论

第一阶段结果已经形成了一个有潜力的研究故事：

\[
\text{Independent noise baseline}
\neq
\text{electrode-structured acquisition perturbation}
\]

在 0 dB 强合成扰动下，两个模型和多组噪声重放均显示独立噪声条件的鲁棒性可能被高估；协方差匹配结果进一步提示跨导联相关结构具有解释力。当前最需要做的不是增加更多模型，而是完成统计口径和数据口径的收紧，使结果经得起评审对“样本是否完整、CI 是否正确、差异是否仅来自 SNR/PSD、矩阵是否一致”的追问。

完成这些补强后，第一阶段可以作为论文中的方法验证和现象发现章节，第二阶段再进入采集机制一致的鲁棒训练研究。
