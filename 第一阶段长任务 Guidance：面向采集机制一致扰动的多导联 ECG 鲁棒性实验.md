# 第一阶段长任务 Guidance：面向采集机制一致扰动的多导联 ECG 鲁棒性实验

## 0. 任务身份与最终目标

你是一个需要连续执行、主动排查问题并保存中间成果的研究型 agent。你的任务不是只给出实验建议，而是**完成第一阶段实验的可复现实验闭环**：数据准备、导联/电极映射建模、噪声生成、基线训练、鲁棒性测试、统计分析、可视化、代码整理和实验报告。

建议至少连续工作 10 小时；如果某一步提前完成，不得直接结束任务，而应继续进行质量检查、敏感性分析、消融实验和文档完善。任何阶段都要将结果保存到磁盘，避免因进程中断丢失成果。

本阶段只验证以下核心假设，不进入最终鲁棒训练方法的全面设计：

> 在控制噪声强度、频谱和单导联噪声幅度后，独立导联噪声与具有电极传播结构的跨导联相关噪声，是否会对多导联 ECG 模型产生不同的性能影响？

核心因果链必须被逐步验证：

```text
电极空间扰动
    -> 导联空间传播
    -> 跨导联协方差/相关性变化
    -> 模型性能与置信度变化
```

本阶段的研究范围限定为 **electrode-induced cross-lead structured perturbation**，即由共享电极和标准导联线性关系导致的结构化跨导联扰动。不得把该模型表述为已经被真实临床电极数据验证的完整采集噪声模型。它不覆盖电极完全脱落、放大器饱和、模数转换故障、设备通信错误等异常。

---

## 1. 必须完成的交付物

任务结束时必须在项目根目录形成以下结构；如果目录名称不同，必须在最终报告中说明实际路径。

```text
phase1_ecg_robustness/
├── README.md
├── configs/
│   ├── phase1_pilot.yaml
│   └── phase1_full.yaml
├── data/
│   ├── README.md
│   ├── download_manifest.json
│   └── processed/                 # 可以是软链接或缓存索引，不强制复制原始数据
├── src/
│   ├── download_ptbxl.py
│   ├── prepare_ptbxl.py
│   ├── lead_matrix.py
│   ├── noise_generators.py
│   ├── covariance_matching.py
│   ├── datasets.py
│   ├── models.py
│   ├── train.py
│   ├── evaluate.py
│   ├── statistics.py
│   └── plotting.py
├── scripts/
│   ├── run_smoke_test.sh
│   ├── run_pilot.sh
│   └── run_full_phase1.sh
├── results/
│   ├── logs/
│   ├── tables/
│   ├── figures/
│   ├── checkpoints/
│   └── metrics/
├── reports/
│   ├── phase1_interim_report.md
│   └── phase1_final_report.md
└── environment.yml or requirements.txt
```

必须至少交付：

1. 可运行的全部实验代码，而不是只保存 notebook。
2. 固定随机种子、配置文件和运行命令。
3. 数据下载与预处理说明。
4. 三类噪声的统计验证结果。
5. 干净测试集和受扰动测试集上的模型结果。
6. 三个以上随机种子的均值、标准差和 95% 置信区间；若计算条件有限，pilot 阶段至少使用 3 个随机种子，full 阶段说明实际数量。
7. 至少 6 张图：原始与受扰动波形、协方差/相关矩阵、PSD、性能-SNR 曲线、电极敏感性图、结果汇总图。
8. 一份中文阶段报告，明确区分已经验证的结果、暂时没有证据的假设和实验限制。

---

## 2. 数据集与官方来源

### 2.1 主数据集：PTB-XL

使用 PTB-XL 1.0.3，官方下载页面：

- https://physionet.org/content/ptb-xl/1.0.3/
- 数据集论文：https://www.nature.com/articles/s41597-020-0495-6

官方页面说明：PTB-XL 包含约 21,799 条临床标准 12 导联 ECG，每条约 10 秒，来自约 18,869 名患者；提供 500 Hz 原始版本和 100 Hz 下采样版本。官方元数据中包含 `patient_id`、`scp_codes`、`filename_hr`、`filename_lr` 以及 `strat_fold`。推荐使用官方 patient-level fold：fold 1–8 训练，fold 9 验证，fold 10 测试。版本号和实际下载文件数量必须记录在 `data/download_manifest.json` 中。

优先使用 100 Hz 版本完成 pilot，以降低计算量；full 阶段可以使用 500 Hz 版本进行敏感性验证。如果使用 100 Hz，必须在报告中说明这是计算效率选择，而不是认为 100 Hz 能完全保留所有高频噪声细节。

不要随机按 ECG 记录划分数据。必须使用 `strat_fold`，并核验同一个 `patient_id` 不跨越 train/validation/test。发现泄漏时必须停止训练并修复。

### 2.2 噪声来源：MIT-BIH Noise Stress Test Database

官方来源：

- https://physionet.org/content/nstdb/1.0.0/

该数据库包含由实际志愿者和标准 ECG 设备、电极采集的噪声记录，包括：

- `bw`：baseline wander；
- `ma`：muscle/EMG artifact；
- `em`：electrode motion artifact。

官方说明中特别指出，electrode motion artifact 可能模拟异位搏动，且通常比一些简单可滤除的噪声更难处理。该数据库还提供不同 SNR 的噪声压力测试记录，可用于核对噪声叠加和 SNR 标定流程。

注意：NSTDB 是噪声记录来源，不代表其中的单导联噪声天然具有 12 导联共享电极结构。使用方式如下：

1. 将其作为基础噪声波形来源；
2. 对独立导联基线，给不同导联独立抽取/随机相位的噪声片段；
3. 对电极空间条件，给电极通道分配噪声，再通过矩阵 `A` 映射到导联；
4. 任何重采样、裁剪和归一化都必须记录。

如果 NSTDB 下载或格式解析失败，先使用可复现的带限高斯噪声完成 smoke test 和 pilot，不得因为外部文件问题中止整个任务；但最终报告必须标注这是回退方案，并优先修复 NSTDB 流程。

### 2.3 数据下载与访问原则

使用 PhysioNet 官方下载入口或官方命令行工具。不要把数据集打包进 Git 仓库，也不要把大文件复制到报告目录。代码应支持以下任一方式：

- `wget`/`curl` 下载官方压缩包；
- `wfdb` Python 包读取 PhysioNet；
- 预先存在的数据目录，通过配置文件传入路径。

README 必须给出：数据集版本、下载网址、许可/使用说明、预计磁盘空间、文件校验或至少文件大小记录。

---

## 3. 标准导联映射矩阵 A 的原理与实现

### 3.1 先明确建模边界

PTB-XL 发布的是已经形成的 12 导联信号，不提供每个电极的原始电势时间序列。因此，本实验不能声称从 PTB-XL 精确恢复了真实电极电势。第一阶段采用的是**标准导联关系诱导的扰动传播模型**：先定义电极空间扰动，再依据标准导联关系把它传播到导联空间。

令电极/节点空间顺序为：

```text
[RA, LA, LL, V1, V2, V3, V4, V5, V6]
```

RL 是右腿驱动/参考电极，在理想差分导联模型中不直接作为诊断电压输入。为了保持“10 个物理电极”的叙述，也可以把 RL 放在第 10 列，但其理想映射列为 0，并在代码注释中说明真实设备中的 driven-right-leg 电路可能更复杂。本研究不模拟 RL 电路动态。

令电极空间扰动为：

\[
\epsilon_e(t)=[\epsilon_{RA}(t),\epsilon_{LA}(t),\epsilon_{LL}(t),
\epsilon_{V1}(t),\ldots,\epsilon_{V6}(t)]^T.
\]

导联扰动为：

\[
n_{lead}(t)=A\epsilon_e(t).
\]

输入 ECG 为：

\[
\widetilde X(t)=X(t)+n_{lead}(t).
\]

### 3.2 推荐的 12×9 矩阵

导联顺序固定为：

```text
[I, II, III, aVR, aVL, aVF, V1, V2, V3, V4, V5, V6]
```

电极顺序固定为：

```text
[RA, LA, LL, V1, V2, V3, V4, V5, V6]
```

在使用“电势差”约定、即导联值为正极减负极的情况下，推荐使用：

```text
A =
[[-1,  1,  0,  0, 0, 0, 0, 0, 0],        # I   = LA - RA
 [-1,  0,  1,  0, 0, 0, 0, 0, 0],        # II  = LL - RA
 [ 0, -1,  1,  0, 0, 0, 0, 0, 0],        # III = LL - LA
 [ 1, -1/2, -1/2, 0, 0, 0, 0, 0, 0],    # aVR = RA - (LA+LL)/2
 [ -1/2, 1, -1/2, 0, 0, 0, 0, 0, 0],    # aVL = LA - (RA+LL)/2
 [ -1/2, -1/2, 1, 0, 0, 0, 0, 0, 0],    # aVF = LL - (RA+LA)/2
 [  1/3,  1/3,  1/3, 1, 0, 0, 0, 0, 0], # V1 - WCT
 [  1/3,  1/3,  1/3, 0, 1, 0, 0, 0],    # V2 - WCT
 [  1/3,  1/3,  1/3, 0, 0, 1, 0, 0, 0],    # V3 - WCT
 [  1/3,  1/3,  1/3, 0, 0, 0, 1, 0, 0],    # V4 - WCT
 [  1/3,  1/3,  1/3, 0, 0, 0, 0, 1, 0],    # V5 - WCT
 [  1/3,  1/3,  1/3, 0, 0, 0, 0, 0, 1],    # V6 - WCT
]
```

这里胸前导联使用 Wilson central terminal：

\[
WCT=(RA+LA+LL)/3.
\]

不同设备和数据转换可能采用不同符号、参考方式或缩放约定。代码必须将矩阵定义写成显式数组，并提供单元测试，不得只在论文文字中口头描述。至少验证：

\[
III=II-I,
\]

并验证增强肢体导联是对应两倍平均关系。符号不影响本研究的协方差思想，但必须在所有实验中保持一致。

### 3.3 矩阵 A 的实现与检查

`src/lead_matrix.py` 必须完成：

1. 返回固定顺序的 `A`；
2. 输出每个导联由哪些电极组成；
3. 提供 `validate_lead_matrix()`；
4. 检查矩阵形状为 `(12, 9)`；
5. 检查标准导联代数关系的残差；
6. 对单位电极扰动逐列传播，打印受影响的导联；
7. 生成一张矩阵热图和一张单位电极扰动传播图。

如果某个实现决定把 RL 加入第 10 列，必须提供 `(12, 10)` 版本，同时保持 RL 列为零，并在报告中说明为什么没有模拟 RL 动态。主实验推荐使用 `(12, 9)`，叙述中说明它对应 10 电极系统中真正参与理想诊断导联差分的 9 个节点。

---

## 4. 三类核心噪声条件与常用方法对照

第一阶段必须至少包含下列三类条件；如果计算资源允许，再加入第四类常用临床噪声条件。

### 4.1 Clean：干净输入

\[
\widetilde X=X.
\]

作用是得到基准性能、校准结果和每个样本的 clean prediction，后续性能下降应优先使用同一样本的配对比较。

### 4.2 Independent Lead Noise：常用独立导联加噪

\[
\widetilde X_{ind}=X+n_{ind}.
\]

对每个导联独立生成噪声，噪声类型可以是：

- 独立高斯白噪声；
- 独立带限高斯噪声；
- 独立 baseline wander；
- 从 NSTDB 抽取、独立分配的 `bw`、`ma` 或 `em` 片段。

这是最重要的常用方法基线，因为许多增强方法本质上默认不同导联可以独立扰动。必须明确记录：不同导联是否使用独立随机种子、是否使用相同 PSD、是否做逐导联 RMS 归一化。

推荐最初用一种频谱明确的带限噪声做主实验，避免同时引入多种混杂因素；然后在 full 阶段加入 `bw`、`ma`、`em` 的敏感性分析。

### 4.3 Electrode-Mapped Noise：电极空间映射噪声

\[
\widetilde X_{elec}=X+A\epsilon_e.
\]

先在电极空间生成 `m=9` 个电极扰动通道，再通过 `A` 映射到 12 导联。推荐三种电极组合：

1. 单电极扰动：一次只扰动 RA、LA、LL 或某一个 V1–V6；
2. 肢体组合扰动：RA/LA/LL 同时扰动；
3. 随机多电极扰动：随机选择 2–4 个电极。

pilot 阶段优先单电极扰动和全电极扰动两种极端情况。单电极扰动便于解释传播关系，全电极扰动便于模拟更普遍的采集变化。

### 4.4 Covariance-Matched Lead Noise：协方差匹配导联空间对照

为了区分“电极映射过程”和“最终跨导联协方差结构”哪个起主要作用，增加：

\[
\widetilde X_{cov}=X+n_{cov},
\quad
Cov(n_{cov})\approx A\Sigma_eA^T.
\]

该条件直接在导联空间生成具有目标协方差的噪声，但不经过电极空间。可使用 Cholesky 分解或特征值分解构造：

\[
n_{cov}=Lz,
\quad LL^T=\Sigma_{target}.
\]

如果目标协方差半正定但数值上有很小的负特征值，应加入明确记录的 jitter，例如 `1e-6` 倍单位矩阵。必须报告：

- `Cov(A epsilon)`；
- `Cov(n_cov)`；
- 二者的 Frobenius 相对误差；
- 相关矩阵的平均绝对误差。

### 4.5 Conventional Clinical Noise：常用临床噪声对照

如果时间和计算资源允许，使用 NSTDB 的 `bw`、`ma`、`em` 作为传统临床噪声。这里要区分两种设置：

- **独立临床噪声**：每个导联独立抽取噪声片段；
- **共享电极临床噪声**：将同一或相关噪声源分配到电极通道后使用 `A` 映射。

如果无法合理地把 NSTDB 噪声解释为电极级噪声，不能伪称它是真实电极噪声；应把它标记为“基于实际采集噪声记录的结构化回放近似”。

### 4.6 不应混淆的对照

以下方法可以作为扩展，但不是本阶段核心证据：

- lead dropout；
- lead masking；
- random time masking；
- amplitude scaling；
- time shift；
- mixup；
- 模型内部 dropout。

它们测试的是缺失、时域变形或训练正则化，不等同于跨导联相关噪声。可以在附加实验中列出，但不得用它们替代 Independent Lead Noise 和 Electrode-Mapped Noise 的核心对照。

---

## 5. 噪声强度、频谱和公平比较

### 5.1 信号功率和 SNR

对每一条 ECG 和每一个扰动样本计算：

\[
P_x=\frac{1}{12T}\sum_{l=1}^{12}\sum_{t=1}^{T}x_l(t)^2.
\]

对噪声计算：

\[
P_n=\frac{1}{12T}\sum_{l=1}^{12}\sum_{t=1}^{T}n_l(t)^2.
\]

使用：

\[
SNR_{dB}=10\log_{10}(P_x/P_n).
\]

主实验推荐 SNR 为 `20 dB, 10 dB, 0 dB`；如果 0 dB 导致信号完全失真，可改为 `20 dB, 15 dB, 10 dB`，但必须在配置和报告中说明。

### 5.2 归一化策略

对每条记录、每个扰动实例，以目标全 12 导联 SNR 进行全局缩放。然后报告每个导联的实际 RMS 和实际 SNR 分布。不能只把一个全局标量写进日志而不验证实际结果。

为了避免差异来自幅值不平衡，至少完成一组“逐导联 RMS 匹配”实验：使 independent 与 electrode-mapped 两种噪声在每个 lead 上具有接近的 RMS，然后只保留其协方差结构差异。

### 5.3 PSD 控制

对主噪声类型，使用相同基础频谱模板：

- 独立条件：各 lead 使用同一个模板但独立随机实现；
- 电极条件：各电极使用同一个模板或规定的电极相关模板，再映射到 leads；
- 协方差匹配条件：从电极映射噪声的经验协方差中构造目标。

使用 Welch 方法估计 PSD，并在 `results/tables/` 保存频带内平均功率。不要声称两个噪声的逐频点 PSD 完全相同，除非代码实际完成了频域匹配；应使用“在预设频带内近似匹配”。

---

## 6. 任务、模型和数据规模

### 6.1 第一阶段任务

优先使用 PTB-XL diagnostic superclass 多标签分类。标签为：

- NORM；
- MI；
- STTC；
- CD；
- HYP。

从 `scp_codes` 和 `scp_statements.csv` 构建标签时，必须按照官方映射和 likelihood 规则处理，不得简单把原始字典字符串直接送入模型。报告中说明每个 superclass 的正样本数和标签共现情况。

如果多标签任务在最初实现阶段不稳定，可以先做一个二分类 smoke test，例如 NORM vs non-NORM，但 full pilot 必须回到预定的多标签任务，或清楚说明为什么改变任务。

### 6.2 模型

第一阶段主模型只使用一个容易复现的 1D CNN 或 1D ResNet。推荐 1D ResNet-18 风格模型，输入形状为 `(12, T)`，输出 5 个 sigmoid logits。

为了验证现象不依赖单一架构，full 阶段再加入一个轻量 Transformer 或 TCN；但不能为了满足“模型多样性”而牺牲核心噪声对照的完整性。

### 6.3 数据规模

分两级执行：

#### Smoke test

- 训练 100–500 条；
- 验证 100 条；
- 测试 100 条；
- 1 个随机种子；
- 1 个 epoch 或少量 steps；
- 目的：检查数据格式、标签、矩阵、梯度、指标和保存流程。

#### Pilot

- 使用官方 train/validation/test fold；
- 可以先用训练集 2,000–5,000 条快速验证流程；
- 验证集和测试集应尽量使用固定子集，保证不同噪声条件共享同一批样本；
- 3 个随机种子；
- 3 个 SNR；
- Clean、Independent、Electrode-mapped、Covariance-matched 四种条件。

#### Full phase 1

- 尽可能使用完整官方训练集；
- 至少 3 个随机种子，理想为 5 个；
- 对每个测试样本固定噪声随机种子，实现配对比较；
- 在主结果稳定后增加 NSTDB 噪声类型或第二个模型。

---

## 7. 评价指标与统计检验

### 7.1 分类指标

多标签任务的主指标：

1. Macro-AUROC；
2. Macro-Average Precision；
3. Macro-F1。

报告 micro 指标作为补充，但不能只报告 micro-F1。Accuracy 不是本阶段主指标，除非任务明确转换为单标签分类。

### 7.2 鲁棒性指标

对每个条件计算：

\[
Drop(M)=M_{clean}-M_{noise}.
\]

以及归一化鲁棒性：

\[
R(M)=\frac{M_{noise}}{M_{clean}+\epsilon}.
\]

同时报告：

- 不同 SNR 下的性能曲线；
- 每个 superclass 的性能下降；
- 每个电极扰动的性能下降；
- 预测置信度和校准误差变化；
- clean/noisy 预测一致性。

### 7.3 结构指标

必须计算：

- 12×12 噪声协方差矩阵；
- 12×12 相关系数矩阵；
- 非对角元素绝对值平均值；
- 与目标协方差的 Frobenius 相对误差；
- 每个 lead 的 RMS、方差和实际 SNR；
- Welch PSD；
- 电极到导联传播的矩阵热图。

### 7.4 统计检验

对于同一条 clean ECG 在不同噪声条件下的结果，优先使用配对分析：

- per-record loss 差异；
- per-record predicted probability 差异；
- bootstrap 95% CI；
- paired permutation test 或 Wilcoxon signed-rank test。

不能只报告一次训练的一组 AUROC 并声称“显著”。模型级指标应报告均值、标准差和置信区间；样本级配对差异应报告效应量和置信区间。

如果比较多个模型的鲁棒性排名，使用 Spearman 或 Kendall rank correlation，并保存排名表。不要只凭肉眼说“排名改变”。

---

## 8. 推荐的执行顺序与长任务节奏

### 阶段 1：环境和数据检查

1. 查看可用 GPU、CPU、内存和磁盘。
2. 创建项目目录。
3. 安装或核验 `wfdb`、`numpy`、`scipy`、`pandas`、`scikit-learn`、`matplotlib`、`seaborn`、`torch`。
4. 下载或定位 PTB-XL 1.0.3。
5. 下载或定位 NSTDB 1.0.0。
6. 保存 `download_manifest.json`。
7. 读取 3–5 条 ECG，检查形状、采样率、单位和异常值。
8. 将所有环境命令和错误写入 `results/logs/setup.log`。

### 阶段 2：标签和划分

1. 解析 `ptbxl_database.csv`。
2. 解析 `scp_statements.csv`。
3. 构建 superclass 标签。
4. 检查空标签、全零标签、类别数量和共现矩阵。
5. 核验官方 fold 与 patient_id 不泄漏。
6. 保存 `results/tables/dataset_summary.csv`。
7. 编写数据加载单元测试。

### 阶段 3：矩阵 A 与噪声模块

1. 实现 `lead_matrix.py`。
2. 写矩阵关系单元测试。
3. 实现独立导联噪声。
4. 实现电极空间噪声。
5. 实现协方差匹配导联噪声。
6. 实现 SNR 标定和逐导联 RMS 统计。
7. 生成波形、PSD、协方差和相关矩阵图。
8. 如果统计控制不达标，不得进入训练；先修复归一化。

### 阶段 4：smoke test

1. 运行 `scripts/run_smoke_test.sh`。
2. 确认 loss 下降或至少能正常反向传播。
3. 确认 clean 与 noisy 输入形状一致。
4. 确认指标没有 NaN。
5. 确认 checkpoint、config 和日志成功保存。
6. 故意对一个样本注入已知单电极扰动，确认影响导联与矩阵理论一致。

### 阶段 5：pilot 训练与测试

1. 固定 3 个随机种子。
2. 训练 clean baseline。
3. 只在测试时施加四种噪声，先避免训练增强混入结论。
4. 对相同测试 ECG 生成配对的四种输入。
5. 保存每个样本和每个模型的预测结果，不仅保存汇总指标。
6. 绘制性能-SNR 曲线和条件间差异。

### 阶段 6：诊断与扩展

只有在 pilot 主结果完成后，才能进行：

1. 单电极敏感性分析；
2. 不同噪声类型分析；
3. 第二个模型；
4. NSTDB 回放；
5. full 数据规模；
6. 模型鲁棒性排名一致性分析。

### 阶段 7：报告与复核

报告必须包含：

1. 研究问题和范围；
2. 数据集来源和版本；
3. A 的定义、公式和代码路径；
4. 各类噪声的生成过程；
5. 公平比较和 SNR/PSD 控制；
6. 模型、任务、划分和随机种子；
7. 结果表和图；
8. 失败实验和修复过程；
9. 局限性；
10. 对第二阶段的明确建议。

---

## 9. 关键验收标准

### 9.1 数据验收

- PTB-XL 版本、来源和文件路径已记录；
- 记录数量和患者数量与官方说明大致一致；
- 采样率正确；
- 官方 fold 被使用；
- 无 patient leakage；
- 标签分布已保存。

### 9.2 矩阵验收

- `A` 的形状和导联/电极顺序明确；
- `III=II-I` 等关系通过单元测试；
- 单位电极扰动传播符合预期；
- 代码中不存在没有解释的符号翻转；
- 论文报告不会把模拟电极空间误写成真实电极观测。

### 9.3 噪声验收

- Independent 条件的非对角相关性接近零；
- Electrode-mapped 条件出现理论预期的跨导联相关性；
- 各条件的实际 SNR 位于目标附近；
- 逐导联 RMS 差异已量化；
- PSD 和协方差统计结果已保存；
- Covariance-matched 条件和 Electrode-mapped 条件的协方差误差可解释。

### 9.4 模型验收

- clean baseline 能正常训练；
- 结果不为 NaN；
- 至少一个 clean checkpoint 和完整日志可复现；
- noisy evaluation 使用相同测试样本；
- 每次实验都保存配置和随机种子。

### 9.5 研究结论验收

只有当噪声统计控制通过后，才可以讨论模型性能差异。如果 Electrode-mapped 与 Independent 的性能不同，必须进一步排查：

1. 总噪声能量是否匹配；
2. 每个 lead 的 RMS 是否匹配；
3. PSD 是否差异过大；
4. 是否只有某个随机种子出现差异；
5. 是否由个别类别或少数样本造成；
6. 是否存在数据预处理或标签泄漏问题。

如果没有性能差异，不得修改结果以制造显著性。应报告“在当前任务、模型和噪声范围内未观察到稳定差异”，然后继续检查更强 SNR、单电极扰动、不同噪声类型和第二个模型。

---

## 10. 失败回退策略

### 数据下载失败

先使用已存在缓存；没有缓存时使用官方 URL 下载。若网络暂时失败，保存错误日志，继续开发矩阵和噪声单元测试。不得伪造实验结果。

### NSTDB 解析失败

先用带限高斯噪声完成完整结构验证，再独立修复 NSTDB。最终报告明确区分 synthetic noise 和 NSTDB noise。

### GPU 不可用

改用 PTB-XL 100 Hz、较短 pilot 子集和轻量 1D CNN。保持实验逻辑和随机种子不变。

### 协方差矩阵不正定

使用对称化：

\[
\Sigma\leftarrow(\Sigma+\Sigma^T)/2
\]

再加最小 jitter。记录 jitter 数值和最小特征值。

### 电极映射噪声过于集中到个别导联

不要直接删除结果。检查这是否正是矩阵传播带来的预期结构。若需要公平比较，增加逐导联 RMS 匹配版本，并同时报告“物理传播版本”和“能量匹配版本”。

### 模型训练不稳定

先降低学习率、检查归一化、缩短输入、确认标签和 loss。不要在未确认数据管线正确前更换模型架构。

---

## 11. 禁止事项

1. 不得把随机按记录切分的数据作为主结果。
2. 不得只在独立噪声上训练和测试，然后声称完成了采集一致性鲁棒性研究。
3. 不得仅凭一个 SNR 和一次随机种子下的结果下结论。
4. 不得把导联 dropout 等同于电极空间噪声。
5. 不得把 A 写成抽象符号而不提供数值矩阵或明确生成规则。
6. 不得同时改变噪声类型、噪声能量、采样率和预处理后再解释为相关结构效应。
7. 不得删除不支持假设的结果。
8. 不得把 PTB-XL 的 12 导联信号说成已经包含原始电极电势。
9. 不得把 pilot 结果包装为真实临床部署验证。
10. 不得在没有保存代码、配置、日志和原始指标的情况下只提交几张最终图片。

---

## 12. 最终报告需要回答的研究问题

最终报告必须明确回答：

1. A 是否按照标准导联关系实现，并产生可解释的跨导联传播？
2. Independent、Electrode-mapped 和 Covariance-matched 三种噪声的统计结构是否确实不同或按设计相近？
3. 在相同或近似相同的 SNR、PSD 和逐导联 RMS 下，模型性能下降是否存在差异？
4. 差异是否在多个随机种子、多个 SNR 或多个噪声类型下保持方向一致？
5. 哪些电极或导联受到的影响最大？
6. 当前证据是否足以支持“独立导联噪声可能造成鲁棒性评价偏差”？
7. 还需要什么实验，才能进入第二阶段的采集一致性鲁棒训练？

最终结论必须使用谨慎措辞：

- 若证据充分：可以说“结果支持跨导联相关结构会影响当前任务下的鲁棒性评价”。
- 若证据有限：可以说“结果表明该机制值得进一步研究，但尚不足以证明普遍性的评价偏差”。
- 若没有差异：应如实说明“当前实验条件下未观察到稳定差异”，并提出下一步改变噪声类型、强度或任务的计划。

---

## 13. 参考来源

[1]: https://physionet.org/content/ptb-xl/1.0.3/ "PTB-XL official PhysioNet dataset page"

[2]: https://www.nature.com/articles/s41597-020-0495-6 "PTB-XL, a large publicly available electrocardiography dataset"

[3]: https://physionet.org/content/nstdb/1.0.0/ "MIT-BIH Noise Stress Test Database official PhysioNet page"

[4]: https://physionet.org/content/mitdb/1.0.0/ "MIT-BIH Arrhythmia Database official PhysioNet page"

[5]: https://wfdb.io/ "WFDB software and waveform database tools"

---

## 14. 给执行 agent 的最后指令

不要在完成一个脚本后立即结束。你的工作完成条件是：

1. 代码可以从 README 的命令重新运行；
2. 至少一个 smoke test 和一个 pilot 已成功完成；
3. 结果文件、图、表和日志真实存在；
4. 统计控制和矩阵单元测试通过；
5. 最终报告能够区分支持性证据、负结果和实验限制；
6. 你已经尝试过至少一次合理的扩展或错误诊断，而不是只运行最小示例。

如果某个阶段失败，先记录原因、保留日志、采用本文件规定的回退方案，并继续向可验收的最小完整结果推进。不能用“环境有问题”作为无实验交付的理由，除非已经明确记录所有尝试和客观阻塞。

任务结束时，在最终回复中给出：

- 项目目录；
- 实际运行命令；
- 已完成实验；
- 主要结果表；
- 失败或未完成项目；
- 对第二阶段的建议；
- 所有关键文件的绝对路径。

**本 guidance 的最高优先级是可复现、可证伪和不夸大结论，而不是制造显著结果。**
